services:
  studio:
    build: ../yt-clipper-studio
    environment:
      VITE_API_BASE_URL: ${PUBLIC_API_BASE_URL:-http://localhost:8000}
      VITE_API_KEY: ${FRONTEND_API_KEY:-dev-secret-change-me}
    ports:
      - "${FRONTEND_PORT:-8080}:80"
    depends_on:
      - api
